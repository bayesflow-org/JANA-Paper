Features a single, self-contained notebook ``joint_training.ipynb'', which applies JANA to a Bayesian denoising problem constructed from the Fashion MNIST data set. 

The only requirement is that the BayesFlow library is installed (https://github.com/stefanradev93/BayesFlow), along with all dependencies.